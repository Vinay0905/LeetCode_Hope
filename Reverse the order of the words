import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String S = scanner.nextLine().trim();  // Read input and remove extra spaces
        scanner.close();

        // Split the string into words (handles multiple spaces)
        String[] words = S.split("\\s+");

        // Build the result by printing words in reverse order
        StringBuilder output = new StringBuilder();
        for (int i = words.length - 1; i >= 0; i--) {
            output.append(words[i]);
            if (i > 0) {
                output.append(" ");  // Add space between words
            }
        }

        // Print the result
        System.out.println(output.toString());
    }
}
