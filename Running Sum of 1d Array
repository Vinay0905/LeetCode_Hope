class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int n=nums.size(),sum=0;
        vector<int> nums1(n);
        for(int i=0;i<n;i++){
            sum=sum+nums[i];
            nums1[i]=sum;
        }
    return nums1;
    }
    
};
